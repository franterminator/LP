#include <stdio.h>

//bucle con variables enteras

int main(int argc, char *argv[])
{
	int start;
	int step = 1;
	int fin = 10;
  double x;

  for ( start = 0; start < fin; start++) {
  x = (double)start/(double)step;
  printf("\n El valor de x es %20.9f\n",x);
  }

  return 0;
}
